# FastCP runtime directory: PHP-FPM sockets and pid files, plus the nginx and
# Apache pid files. Each unit also declares RuntimeDirectory=fcp, so this is
# belt and braces: it means the directory exists from boot regardless of which
# service starts first, or whether one is started by hand.
d /run/fcp 0755 root root -
